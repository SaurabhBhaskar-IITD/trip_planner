// Test stub for the `server-only` package (see vitest.config.ts alias).
// The real package intentionally throws when bundled for the client; under Node
// test we replace it with a no-op so server modules are importable.
export {};
